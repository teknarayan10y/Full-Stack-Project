Only Full Stack projects are included in this repository.
Before running any project, first install the required dependencies using:

npm install

Then start the project using the appropriate command (for example):

npm start

or

npm run dev
